<view class="page-shell columns-page">
  <view class="columns-hero">
    <view class="columns-kicker">独立工作者专栏</view>
    <view class="columns-title">把独立工作写成可持续的方法</view>
    <view class="columns-copy">
      这里整理小红书运营、AI 工具实践和独立思考相关内容，所有内容均为正式公开阅读栏目。
    </view>
  </view>

  <view class="column-list">
    <view
      wx:for="{{columns}}"
      wx:key="id"
      class="column-card"
      bindtap="openColumn"
      data-id="{{item.id}}"
    >
      <view class="column-icon">{{item.icon}}</view>
      <view class="column-body">
        <view class="column-title">{{item.title}}</view>
        <view class="column-desc">{{item.description}}</view>
        <view class="column-meta">{{item.meta}}</view>
      </view>
    </view>
  </view>

  <view class="notice-card">
    <view class="notice-title">内容说明</view>
    <view class="notice-text">
      当前小程序提供公开博客、图文专栏与文章阅读服务；如后续开放付费内容，将按平台要求补充明确商品信息、价格和服务说明。
    </view>
  </view>
</view>
